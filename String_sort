#include <iostream>
#include <vector>
#include <climits>
#include <string>
#include <algorithm>
#include <map>
using namespace std;

int main() {
    int n;
    string s;
    cin >> n;
    map<int, string> p;
    for(unsigned i = 0; i < n; i++){
        cin >> s;
        if ((char)s[0] )
        //p[(int)s[0]] = s;
    }
    /*
    for(map<int, string>::iterator j = p.begin(); j != p.end(); j++){
        cout << j->first << " : " << j->second << endl;
    }
    */
}
